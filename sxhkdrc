# Lock Screen
super + l
	slock
# Screenshot
super + s
	scrot -s -F screenshot.png -o && kolourpaint screenshot.png
# Mute / Unmute Audio
XF86AudioMute
#	pamixer -t && notify-send "Audio Toggle" "TEST"
	~/.dotfileRepos/sxhkd/toggleAudio.sh
